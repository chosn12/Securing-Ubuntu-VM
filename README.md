# Ubuntu VM Security Hardening

## 1. Update:
   - Terminal
     
     *`sudo apt update && sudo apt upgrade -y`*

## 2. Firewall:
   - Terminal

     `sudo ufw enable`
     
     `sudo ufw default deny incoming`
     
     `sudo ufw allow ssh`


## 3. 
     
